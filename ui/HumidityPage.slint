import {Button, VerticalBox } from "std-widgets.slint";

export component HumidityPage inherits Rectangle {
    VerticalLayout {

        spacing: 10px;
        
        Button {
            text: "High";
        }

        Button {
            text: "Medium";
        }

        Button {
            text: "Low";
        }
    }
}
